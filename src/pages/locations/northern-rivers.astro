---
// northern-rivers region page. The layout and every word of the copy live in
// src/layouts/RegionPage.astro and src/data/regionPages.ts respectively.
// This file exists only to claim the URL: a static route beats the
// /locations/[town].astro dynamic sibling without ambiguity, which two
// dynamic routes in one directory could not do. See RegionPage.astro.
import RegionPage from "../../layouts/RegionPage.astro";
---

<RegionPage slug="northern-rivers" />
